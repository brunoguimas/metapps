$kind: http-request
name: Get Current User
method: GET
url: '{{backendBaseUrl}}/auth/me'
order: 4000
headers:
  - key: Accept
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
