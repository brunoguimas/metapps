$kind: http-request
name: Verify Email
method: POST
url: "{{backendBaseUrl}}/auth/email/verify"
body:
  type: json
  content: |-
    {
        "email": "brunoguimaraes.guigui@gmail.com",
        "code": "194263"
    }
order: 5000
headers:
  Accept: application/json
queryParams:
  - key: token
    value: sample-verification-token
    description: Email verification token received in the verification email
